import type { LevelComposer } from '../composerBridge';

const C1_N5: LevelComposer = {
  id: 'C1_N5',
  pieces: [
    {
      kind: 'MANDATORY',
      key: 'c1n5_m1',
      label: 'Estrutura sintática excelente (no máximo, uma falha) E, no máximo, dois desvios',
      noId: true,
    },
  ],
};

export default C1_N5;
