{
  "name": "site-professor-yago",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["frontend"],
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm --workspace frontend run build",
    "dev": "npm --workspace frontend run dev"
  }
}
